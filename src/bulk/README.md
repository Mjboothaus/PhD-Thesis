## WARNING

This is an attempted re-factoring of the original code PyOZ for bulk fluids.

The re-factoring is in-progress; likely incomplete and completely untested.

The idea is to call this code to produce results for the bulk fluid if required else provide them from the cached results.

The original code archive and associated files can be found here:

- `./src/pyoz/pyoz-0.3.0.tar.bz2`